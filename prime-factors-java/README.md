# prime-factors-java

`JUnit 4` and `Hamcrest` dependencies are defined in `pom.xml`.

Project layout:

- `src/main/java/PrimeFactors.java`
- `src/test/java/PrimeFactorsTest.java`

Run with Maven after it is installed:

```bash
mvn test
```
