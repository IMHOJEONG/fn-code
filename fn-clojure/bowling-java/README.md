# bowling-java

`JUnit 4` and `Hamcrest` are configured in `pom.xml`.

Project layout:

- `src/main/java/BowlingGame.java`
- `src/test/java/BowlingGameTest.java`

Run tests with:

```bash
mvn -Dmaven.repo.local=.m2 test
```
